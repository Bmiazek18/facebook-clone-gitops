apiVersion: v1
kind: Service
metadata:
  name: postgres-keycloak
  namespace: {{ .Release.Namespace | default "apps" }}
  labels:
    app: postgres-keycloak
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app: postgres-keycloak